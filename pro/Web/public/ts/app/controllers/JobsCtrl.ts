/// <reference path="../reference.ts" />

namespace Peach {
	"use strict";

	export class JobsController {
		static $inject = [
			C.Angular.$scope
		];

		constructor(
			$scope: IViewModelScope
		) {
		}
	}
}
