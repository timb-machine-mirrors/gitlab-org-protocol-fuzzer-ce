include::NTP_DataSheet.txt[]

Configuration
-------------

.Target Configuration
A UDP listener listening on the UDPv4 port defined in configuration file is required. The network tool socat can be used.

_Example of using socat to listen on UDPv4 port:_

----
socat STDIO udp4-listen:12345
----

.Required Pit Configuration Changes
SourceMAC:: MAC address on local machine
TargetMAC:: MAC address of target machine
Interface:: Name of local interface
ActorPort:: Port number of the local machine
PartnerPort:: Port number of the target host machine

.Optional Pit Configuration Changes
Strategy:: Fuzzing strategy Peach will use for testing
LoggerPath:: Path to folder where logs will be stored

.Sample NTP Configuration File
[source,xml]
----
<?xml version="1.0" encoding="utf-8"?>
<PitDefines>
  <All>
    <Define key="SourceMAC"     value="5254005335d3"/>
    <Define key="TargetMAC"     value="000000000000"/>
    <Define key="LoggerPath"    value="./logs/arp/"/>
    <Define key="ActorPort"     value="0012"/>
    <Define key="PartnerPort"   value="0000"/>
    <Define key="Strategy"      value="Sequential"/>
    <Define key="Path"          value="."/>
  </All>

  <Linux platform="linux">
    <Define key="Interface"       value="eth0"/>
    <Define key="tsharkInterface" value="eth0"/>
    <Define key="tsharkBin"       value="/usr/bin/tshark"/>
  </Linux>

  <OSX platform="osx">
    <!-- This can't run on osx because it uses the RawEther publisher. -->
  </OSX>

  <Windows platform="windows">
    <!-- This can't run on windows because it uses the RawEther publisher. -->
  </Windows>
</PitDefines>

----

.Configure Additional Monitors
Add monitors to agent as needed

Running
-------

.Single test debug run
----
peach -1 -debug NTP.xml
----
.Full test run
----
peach NTP.xml
----